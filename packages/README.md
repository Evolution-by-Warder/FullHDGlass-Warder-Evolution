# Update packages

FullHDGlass17 Warder Evolution update packages are stored as ordinary repository files and are downloaded directly through raw GitHub URLs.

GitHub Releases are intentionally not used for updater packages.

The active package URL and SHA-256 checksum are published in `/update.json`.
